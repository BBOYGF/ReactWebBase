package com.telecwin.bee.core.extractor.transformer;

import com.telecwin.bee.core.extractor.DataSetTransformerBase;

/**
 * 列(字段)选择器
 * <p>
 * 根据用户选择的维度、指标，向 DataSet 添加列。
 * 主要是用于选择主表的字段，而“字段设置器”决定更多的列属性。
 * </p>
 */
public class ColumnSelector extends DataSetTransformerBase {
}
